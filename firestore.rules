rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /plan_trabajo/{document=**} {
      allow read, write: if true;
    }
    match /admin_settings/{document=**} {
      allow read, write: if true;
    }
  }
}
